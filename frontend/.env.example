# Development (Vite proxy handles /api → crm_backend:8000, no value needed)
VITE_API_BASE_URL=

# Production — set this in the frontend container build args or CI
# VITE_API_BASE_URL=https://api-crm.x1techs.com/api/v1

VITE_APP_NAME="X1Techs CRM"
